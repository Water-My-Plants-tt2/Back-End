const express = require("express");
const router = express.Router();

const Plant = require("./plants.model");

// authenticated user can create plants

// authenticated user can update plants

// authenticated user can delete plants 💀

// authenticated user can view their created plants

module.exports = router;
